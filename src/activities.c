#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "activities.h"
#include "graph.h"

#define MAX_ACTIVITIES 100

static Activity activities[MAX_ACTIVITIES];
static int activity_count = 0;

void add_activity(Activity** head, char* name){
    // ajouter une nouvelle activité
}

void remove_activity(Activity** head, char* name){
    // l'activité a supprimmer 
}

void recommend_activities(Activity* head, const char* preference) {
   // l'activité a recommander 
}